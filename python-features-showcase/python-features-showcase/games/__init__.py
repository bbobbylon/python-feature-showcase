"""
games package
=============

Three tiny, fully playable terminal games. Each one was picked to
highlight a different set of Python features in a fun, low-stakes way:

    - number_guesser.py    -> loops, the `random` module, conditionals
    - rock_paper_scissors.py -> dicts as lookup tables, loops, randomness
    - tic_tac_toe.py       -> 2D data structures, functions, a simple
                               "game loop" pattern, and light OOP

None of these need any third-party libraries -- everything comes from
Python's standard library, so they'll run anywhere Python 3.10+ is
installed.
"""
