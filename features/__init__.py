"""
features package
=================

This package is the heart of the "feature tour" part of the app. Each module
inside it focuses on ONE cluster of Python language features, and every
function is written to be *run* (so you see real output) as well as *read*
(so the comments teach you what's going on).

Analogy for readers new to Python:
    Think of this package like a box of labeled drawers (a "toolbox").
    Each drawer (module) holds tools for a related job:
        - basics_and_datatypes.py   -> the raw materials (numbers, text, booleans)
        - collections_and_comprehensions.py -> containers to hold many things
        - functions_and_decorators.py -> reusable tools, and "wrappers" for tools
        - oop_showcase.py            -> blueprints for building your own objects
        - iterators_and_generators.py -> conveyor belts that produce items one at a time
        - error_handling.py          -> safety nets for when things go wrong

If you're coming from another language: Python favors *readability* and
*duck typing* ("if it walks like a duck and quacks like a duck, treat it
like a duck") over the strict type declarations you might be used to in
languages like Java or C#. Type hints (see modern Python features) let you
opt into more explicit typing when you want it, without forcing it.
"""
